{
    "name": "lite-i18n-esbuild-example",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "node build.js",
        "dev": "node build.js --watch",
        "serve": "npm run build && npx serve ."
    },
    "dependencies": {
        "lite-i18n": "file:../../"
    },
    "devDependencies": {
        "esbuild": "^0.20.0"
    }
}